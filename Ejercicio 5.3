# Ejercicio 5.3: ("De una lista de pares, definir una funcion que devuelva la suma de solo los valores impares")

numeros = list(range(1, 11))
print(numeros)
impares = [numero for numero in numeros if numero %2 == 1]

print(impares)







